const base = 'https://www.bootcampspot.com/api/instructor/v1/'

const Routes = {
    login: `${base}login`,
    me: `${base}me`,
    attendance: `${base}attendance`,
    sessions: `${base}sessions`
}

export { Routes }